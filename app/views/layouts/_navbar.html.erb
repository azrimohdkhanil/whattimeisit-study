<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
	<div class="navbar-header">
	  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
		<span class="sr-only">Toggle navigation</span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
		<span class="icon-bar"></span>
	  </button>
	  <a class="navbar-brand" href="/">What Time Is It?</a>
	</div>
	<div id="navbar" class="collapse navbar-collapse">
	  <ul class="nav navbar-nav">
		<li <%= current_page?(root_path)? "class=active" : "" %> ><a href="<%= root_path %>">Home</a></li>
		<li <%= current_page?(controller:'static_pages', action:'about')? "class=active" : "" %>><a href="<%= about_path %>">About</a></li>
		<li <%= current_page?(controller:'static_pages', action:'contact')? "class=active" : "" %>><a href="<%= contact_path %>">Contact</a></li>
		<% if is_admin? %>
		<li <%= current_page?(controller:'users', action:'index')? "class=active" : "" %>><a href="<%= users_path %>">Users</a></li>
		<% end %>
	  </ul>
	  
	  <ul class="nav navbar-nav navbar-right">
		    <% 
			  if logged_in?
				  the_link = link_to 'Sign Out', sign_out_path, {method: :delete}
			  else
				  the_link = link_to 'Sign In', sign_in_path
				  sign_up = link_to 'Sign Up', sign_up_path
			%>	  
                  <li><%= sign_up %></li>
                  <li class="divider"></li>
			<%
			  end
            %>
		  <li><%=  the_link %></li>
	  </ul>
	</div><!--/.nav-collapse -->
  </div>
</nav>